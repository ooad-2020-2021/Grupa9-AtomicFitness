@model IEnumerable<AtomicFitness.Models.Pjesma>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-secondary">Create New</a>
</p>

<div style="margin-bottom: 10px">
    @using (Html.BeginForm("Index", "Pjesma", FormMethod.Get))
    {
        <div style="margin-bottom: 10px">
            <b style="margin-right: 10px">Search By:</b>
            @Html.RadioButton("SearchBy", "Naziv", true) <span style="margin-right: 10px">Name</span>
            @Html.RadioButton("SearchBy", "Pjevaci") <span style="margin-right: 10px">Singers</span>
            @Html.RadioButton("SearchBy", "Zanr") <span style="margin-right: 10px">Genre</span>
        </div>
        @Html.TextBox("Search") <input type="submit" value="Search"/>
        <br />
        <small class="form-text text-muted">For more than one ingredient use comma as a delimiter</small>
    }
</div>
<table class="table table-hover" style="text-align: center">
    <thead>
        <tr class="table-dark">
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pjevaci)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zanr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GodinaIzdanja)
            </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@if (Model.Count() == 0)
{
    <tr>
        <td colspan="4">No rows match search criteria</td>
    </tr>

}
else
{
    foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pjevaci)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zanr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GodinaIzdanja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PjesmaID" class="btn btn-outline-primary">Edit</a> 
                <a asp-action="Details" asp-route-id="@item.PjesmaID" class="btn btn-outline-primary">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.PjesmaID" class="btn btn-outline-primary">Delete</a>
            </td>
        </tr>
    }
}
    </tbody>
</table>
