@model IEnumerable<AtomicFitness.Models.Recept>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-secondary">Create New</a>
</p>
<p>
    @using (Html.BeginForm("Index", "Recept", FormMethod.Get))
    {
        <b style="margin-right: 10px">Search By:</b>
        @Html.RadioButton("SearchBy", "Naziv", true) <span style="margin-right: 10px">Name</span>
        @Html.RadioButton("SearchBy", "Sastojci") <span style="margin-right: 10px">Ingredients</span>
        <br />
        @Html.TextBox("Search") <input type="submit" value="Search"/>
        <br />
        <small class="form-text text-muted">For more than one ingredient use comma as a delimiter</small>
    }
</p>
<table class="table table-hover">
    <thead>
        <tr class="table-dark">
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sastojci)
            </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@if (Model.Count() == 0)
{
    <tr>
        <td colspan="3">No rows match search criteria</td>
    </tr>

}
else
{
    foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sastojci)
            </td>
            <td class="btn-group" role="group">
                <a asp-action="Edit" asp-route-id="@item.ReceptID" class="btn btn-outline-primary">Edit</a> 
                <a asp-action="Details" asp-route-id="@item.ReceptID" class="btn btn-outline-primary">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.ReceptID" class="btn btn-outline-primary">Delete</a>
            </td>
        </tr>
    }
}
    </tbody>
</table>
